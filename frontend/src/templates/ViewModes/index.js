/**
 * ViewMode Templates - Consolidated export
 *
 * Generic, configuration-driven view components that eliminate duplicate code
 * across all dashboard view modes (Card, List, Table).
 *
 * Usage:
 * import { CardTemplate, ListItemTemplate, TableRowTemplate } from '../../../templates/ViewModes';
 * import { getFieldValue, formatFieldValue, resolveField } from '../../../templates/ViewModes';
 */

// Components
export { CardTemplate, ListItemTemplate, TableRowTemplate } from './components';

// Utilities
export * from './utils';
