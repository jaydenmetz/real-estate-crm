/**
 * ViewMode Templates - Generic view components
 *
 * These templates eliminate duplicate code across all dashboard view modes.
 * Each dashboard provides a configuration object instead of implementing
 * separate Card/List/Table components.
 */

export { default as CardTemplate } from './CardTemplate';
export { default as ListItemTemplate } from './ListItemTemplate';
export { default as TableRowTemplate } from './TableRowTemplate';
