/**
 * Common View Mode Components
 *
 * Shared view mode components that can be used across all entities.
 * Follows the same structure as entity-specific view modes:
 * - card/ - Card view components (320px fixed width)
 * - list/ - List view components (future)
 * - table/ - Table view components (future)
 */

// Card view components
export {
  ClientCard,
  LeadCard,
  ListingCard,
  AppointmentCard
} from './card';

// List view components (future)
// export { EntityListItem } from './list';

// Table view components (future)
// export { EntityTableRow } from './table';
